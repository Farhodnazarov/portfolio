{
  "source": "/(.*)",
  "destination": "/index.html",
  "rewrites": [{ "source": "/api/(.*)", "destination": "/api/$1" }],
  "builds": [
    {
      "src": "api/**/*.js",
      "use": "@vercel/node"
    }
  ]
}
